<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"

    xmlns:tools="http://schemas.android.com/tools"
    android:configChanges="orientation|screenSize|keyboardHidden"
    android:title="@string/settings_window_title">
    <ListPreference
        android:defaultValue="@string/pref_most_popular_key"
        android:dialogTitle="@string/settings_order_by"
        android:entries="@array/pref_sort_options"
        android:entryValues="@array/pref_sort_values"
        android:key="@string/settings_order_by_key"
        android:summary=""
        android:title="Sort By" />

</PreferenceScreen>